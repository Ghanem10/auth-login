import React from 'react';
import AuthenticationRequests from '../Auth/auth';

export default function App() {
    return (
        <div className="container">
            <AuthenticationRequests />
        </div>
    );
}